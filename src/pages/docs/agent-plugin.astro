---
import DocsLayout from "../../layouts/DocsLayout.astro";

const base = import.meta.env.BASE_URL;
---

<DocsLayout title="AI Agent Plugin" description="Install the Laurel Proxy AI agent plugin for automated HTTP debugging. Your AI coding agent can query captured traffic, inspect API requests, and replay requests to verify fixes." headings={[
  { id: "installation", text: "Installation", depth: 2 },
  { id: "what-it-provides", text: "What It Provides", depth: 2 },
  { id: "example-workflows", text: "Example Workflows", depth: 2 },
  { id: "debug-a-failing-webhook", text: "Debug a failing webhook", depth: 3 },
  { id: "find-why-your-api-returns-422", text: "Find why your API returns 422", depth: 3 },
  { id: "capture-ios-traffic", text: "Capture iOS traffic", depth: 3 },
  { id: "agent-output-mode", text: "Agent Output Mode", depth: 2 },
  { id: "related", text: "Related", depth: 2 },
]}>
  <div class="prose-custom">
    <p>Laurel Proxy ships with an AI agent plugin. Once installed, your AI agent knows all Laurel Proxy commands, filters, API endpoints, and common debugging workflows -- so you can ask it to inspect traffic in natural language.</p>

    <h2 id="installation">Installation</h2>
    <p>The plugin is currently available for <strong>Claude Code</strong>. Add it from the Claude Code plugin marketplace:</p>
    <pre><code>/plugin marketplace add rvanbaalen/laurelproxy</code></pre>
    <p>Then open the plugin browser and install it:</p>
    <pre><code>/plugin</code></pre>
    <p>Support for other AI coding agents (Cursor, Copilot, Windsurf) is planned. In the meantime, any agent can use Laurel Proxy directly via the <a href={`${base}docs/cli/`}>CLI</a> and <a href={`${base}docs/api/`}>REST API</a>.</p>

    <h2 id="what-it-provides">What It Provides</h2>
    <p>After installation, your AI agent can help you:</p>
    <ul>
      <li>Start and stop the proxy and configure <a href={`${base}docs/https/`}>HTTPS interception</a></li>
      <li>Write <code>laurel-proxy requests</code> queries with the right filter flags</li>
      <li>Use the <a href={`${base}docs/api/`}>REST API</a> to query captured traffic programmatically</li>
      <li>Debug failing API calls by inspecting captured request/response pairs</li>
      <li>Set up system-wide proxy routing on macOS</li>
    </ul>
    <p>Just ask your AI agent anything about intercepting or inspecting HTTP traffic and it will use its knowledge of Laurel Proxy to help.</p>

    <h2 id="example-workflows">Example Workflows</h2>

    <h3 id="debug-a-failing-webhook">Debug a failing webhook</h3>
    <p>Tell your AI agent something like: <em>"The Stripe webhook is failing. Debug it."</em></p>
    <p>The agent will start the proxy if needed, query Laurel Proxy for recent requests matching the webhook endpoint, inspect the request headers and response body, and pinpoint what went wrong -- all without you writing a single filter command. See the full <a href={`${base}docs/recipes/debug-webhook/`}>Debug a Failing Webhook recipe</a> for the step-by-step walkthrough.</p>

    <h3 id="find-why-your-api-returns-422">Find why your API returns 422</h3>
    <p>Ask: <em>"Why is my API returning 422?"</em></p>
    <p>The agent filters captured traffic by status code 422, inspects the request body that triggered the error, reads the response error message, and explains what validation failed and how to fix it. The <a href={`${base}docs/recipes/debug-422/`}>Debug 422 Errors recipe</a> covers this workflow in detail.</p>

    <h3 id="capture-ios-traffic">Capture iOS traffic</h3>
    <p>Ask: <em>"Help me set up the proxy to capture traffic from my iPhone."</em></p>
    <p>The agent walks you through starting the proxy, finding your network address, configuring the iOS proxy settings, installing the CA certificate on the device, and enabling full trust -- step by step. Follow the <a href={`${base}docs/recipes/ios-capture/`}>Capture iOS Traffic recipe</a> for the complete guide.</p>

    <h2 id="agent-output-mode">Agent Output Mode</h2>
    <p>For optimal AI consumption, the CLI supports a <code>--format agent</code> output mode. This produces structured output that any LLM can parse efficiently when querying traffic programmatically — it works with Claude Code, Cursor, Copilot, or any other AI agent. Use it when you want your agent to process large amounts of captured traffic:</p>
    <pre><code>laurel-proxy requests --format agent --host api.example.com</code></pre>

    <h2 id="related">Related</h2>
    <ul>
      <li><a href={`${base}docs/getting-started`}>Getting Started</a> - Install and capture your first request</li>
      <li><a href={`${base}docs/cli`}>CLI Reference</a> - Commands your agent uses under the hood</li>
      <li><a href={`${base}docs/recipes`}>Recipes</a> - Step-by-step debugging workflows</li>
    </ul>
  </div>
</DocsLayout>

<style>
  .prose-custom h2 {
    color: var(--color-text-primary);
    font-size: 1.25rem;
    font-weight: 700;
    margin-top: 2rem;
    margin-bottom: 0.75rem;
  }
  .prose-custom h3 {
    color: var(--color-text-primary);
    font-size: 1.0625rem;
    font-weight: 600;
    margin-top: 1.5rem;
    margin-bottom: 0.5rem;
  }
  .prose-custom p {
    margin-bottom: 1rem;
    line-height: 1.7;
  }
  .prose-custom pre {
    background: var(--color-bg-secondary);
    border: 1px solid var(--color-border);
    border-radius: 0.5rem;
    padding: 1rem;
    overflow-x: auto;
    margin-bottom: 1rem;
    font-family: var(--font-mono);
    font-size: 0.8125rem;
    line-height: 1.6;
    color: var(--color-text-secondary);
  }
  .prose-custom code:not(pre code) {
    background: var(--color-bg-secondary);
    border: 1px solid var(--color-border);
    border-radius: 0.25rem;
    padding: 0.125rem 0.375rem;
    font-family: var(--font-mono);
    font-size: 0.8125rem;
  }
  .prose-custom ul {
    list-style: disc;
    padding-left: 1.5rem;
    margin-bottom: 1rem;
  }
  .prose-custom li {
    margin-bottom: 0.5rem;
    line-height: 1.6;
  }
  .prose-custom a {
    color: var(--color-accent);
    text-decoration: underline;
    text-underline-offset: 2px;
  }
  .prose-custom a:hover {
    color: var(--color-text-primary);
  }
  .prose-custom em {
    color: var(--color-accent);
    font-style: normal;
  }
  .prose-custom strong {
    color: var(--color-text-primary);
  }
</style>
