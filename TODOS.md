# TODOS

## Above-the-fold Web UI presence

**What:** Add Web UI presence above the fold (hero subtitle, screenshot preview, or modify one timeline card to show the dashboard instead of terminal output)

**Why:** The hero and all four timeline cards show terminal/CLI output. Scanners who don't scroll past the timeline miss the Web UI story entirely.

**Pros:** Fixes the root cause of the "CLI-only impression" — visitors understand the Web UI exists without scrolling to the bottom half of the page.

**Cons:** Requires changing the hero or timeline sections, which are currently well-designed and working.

**Context:** Raised by Codex outside voice during /plan-eng-review on 2026-03-30. User chose to keep current approach (additive sections below timeline) for this iteration. Consider for a future pass.

**Depends on:** The Web UI + REST API showcase sections landing first (current plan).
